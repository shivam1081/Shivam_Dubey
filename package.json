{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://shivam1081.github.io/react-portfolio",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  }
}
